# 슬라이드 9 — 향후 개발 계획

---

저희가 앞으로 구현할 항목들을 우선순위별로 설명드리겠습니다.

---

## 우선순위 높음 — 앱 정상 동작에 필요한 항목

**첫째, 기존 프로필 자동 불러오기입니다.**
현재는 로그인하면 항상 신체 데이터 입력 화면으로 이동합니다.
개선 후에는 로그인 시 Firebase DB에 저장된 프로필을 먼저 조회하여,
프로필이 있으면 홈 화면으로 바로 이동하고, 없을 때만 입력 화면으로 이동합니다.

**둘째, 앱 재실행 시 상태 유지입니다.**
현재는 앱을 완전히 종료 후 재실행하면 프로필 상태가 초기화되어 홈 화면의 맞춤 페이스 정보가 표시되지 않습니다.
개선 후에는 앱 시작 시 DB에서 프로필을 자동으로 불러와 Provider에 주입하여, 재실행 후에도 맞춤 페이스 정보가 유지됩니다.

---

## 우선순위 중간 — 주요 UX 개선 항목

**셋째, 신체 데이터 수정 기능입니다.**
현재는 최초 입력 후 수정이 불가능합니다. 홈 화면에 프로필 수정 버튼을 추가하여 언제든지 신체 정보를 업데이트할 수 있도록 개선할 계획입니다.

**넷째, 러닝 기록 삭제 UI입니다.**
삭제 로직은 이미 구현되어 있지만, 홈 화면에 UI가 없는 상태입니다. 스와이프 또는 길게 누르기 방식으로 기록을 삭제할 수 있도록 추가할 예정입니다.

**다섯째, 러닝 기록 상세 보기입니다.**
현재 홈 화면의 기록 카드를 탭해도 반응이 없습니다. 탭 시 상세 화면으로 이동하여 페이스 변화 그래프와 러닝 경로 지도를 확인할 수 있도록 구현할 계획입니다.

**여섯째, 러닝 중 화면 켜짐 유지입니다.**
현재 러닝 중 화면이 꺼지면 GPS 측정이 불안정해질 수 있습니다. WakeLock을 적용하여 러닝 중에는 화면이 꺼지지 않도록 할 예정입니다.

---

## 우선순위 낮음 — 추가 기능 항목

**백그라운드 GPS 추적입니다.**
현재는 앱이 백그라운드로 전환되면 GPS 추적이 중단됩니다. Android Foreground Service를 구현하여 화면이 꺼지거나 다른 앱을 사용하는 중에도 GPS 추적이 유지되도록 개선할 계획입니다.

**러닝 경로 지도 표시입니다.**
러닝 중 수집된 위도·경도 데이터를 결과 화면에서 지도 위에 경로로 시각화할 예정입니다.

**주간·월간 통계 화면입니다.**
개별 기록 외에 주간·월간 총 거리, 평균 페이스 추이 등 누적 통계를 한눈에 확인할 수 있는 화면을 추가할 계획입니다.

**앱 아이콘 및 스플래시 스크린입니다.**
현재 기본 Flutter 아이콘을 RunRight 전용 디자인으로 교체하고, 앱 실행 시 스플래시 화면을 추가할 예정입니다.

**소셜 로그인입니다.**
현재 이메일·비밀번호 방식에 더해 Google 소셜 로그인을 추가하여 가입·로그인 편의성을 높일 계획입니다.

**Firebase 보안 규칙 적용입니다.**
현재 테스트 모드로 운영 중인 Realtime Database에 사용자별 읽기·쓰기 접근만 허용하는 보안 규칙을 적용할 예정입니다.

---

**마지막으로, 러닝 데이터 기반 권장 페이스 점진적 보정입니다.**

현재 RunRight는 신체 데이터를 입력받아 VO2max를 추정하고, 그 결과로 권장 페이스를 고정된 값으로 제시합니다.
그러나 실제 운동 능력은 달릴수록 변화하며, 입력 데이터만으로는 개인의 실제 체력을 완전히 반영하기 어렵습니다.

향후에는 러닝 이후 수집된 실측 데이터, 즉 실제 달린 페이스, 운동 지속 시간, 심박수 등을 누적하여
사용자가 실제로 어느 강도에서 운동을 유지하는지를 분석하고,
이를 바탕으로 권장 페이스와 거리를 점진적으로 조정하는 적응형 추천 구조로 발전시킬 계획입니다.

예를 들어, 사용자가 권장 페이스보다 지속적으로 빠르게 달리면서도 완주를 유지한다면 체력이 향상된 것으로 판단하여 권장 페이스 범위를 상향 조정하고, 반대로 권장 범위 안에서도 러닝을 조기 종료하는 패턴이 반복된다면 강도를 보수적으로 낮추는 방식입니다.

이는 초기 신체 데이터 기반의 정적인 추정값을 실제 러닝 기록으로 지속적으로 보정하는 구조로, 앱을 사용할수록 개인에게 더 정확한 페이스를 제시할 수 있게 됩니다.

---
