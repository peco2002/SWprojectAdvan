# RunRight 발표자료 생성 프롬프트

아래 내용을 바탕으로 대학교 팀 프로젝트 발표용 슬라이드 자료를 만들어줘.

---

## 앱 기본 정보

- **앱 이름**: RunRight
- **주제**: 러닝 입문자를 위한 신체 맞춤형 페이스 코칭 앱
- **플랫폼**: Flutter (Android)
- **백엔드**: Firebase (Authentication + Realtime Database)

---

## 구현된 기능 목록 (슬라이드 구성 기준)

### 1. 회원가입 / 로그인 (Firebase Authentication)
- 이메일 + 비밀번호 기반 회원가입 및 로그인
- 회원가입 시 이름, 이메일, 비밀번호, 비밀번호 확인 입력
- 로그인 상태 실시간 감지 → 자동 화면 전환 (StreamBuilder)
- 한국어 오류 메시지 처리 (이미 사용 중인 이메일, 비밀번호 불일치 등)
- 로그아웃 기능 (홈 화면 상단 버튼)

### 2. 신체 데이터 입력 (3단계 스텝 UI)
- **Step 1 - 기본 신체 정보**: 키(cm), 체중(kg), 나이, 성별(남/여) 입력
- **Step 2 - 운동 경험 선택**: 처음 시작 / 가끔 운동 / 꾸준히 운동 중 선택
- **Step 3 - 인바디 데이터 (선택)**: 체지방률(%), 골격근량(kg) 선택 입력
- 단계별 진행 바(Step Indicator) UI
- 입력값 유효성 검사 (범위 초과, 빈 값 등)
- 입력 완료 후 Firebase Realtime Database에 신체 프로필 자동 저장

### 3. 맞춤 페이스 산출 알고리즘
- **VO2max 추정**:
  - 19~35세 + 체지방률 있음 → 국내 비운동 회귀식 적용 (R²≈0.70)
  - 그 외 → 나이/성별 기반 ACSM 간이식 적용
  - 인바디 데이터(체지방률, 골격근량) 있을 경우 추가 보정
  - 운동 경험 수준에 따른 보정값 적용
- **권장 페이스 범위 산출**:
  - 빠른 한계(템포): VO2max × 85%
  - 느린 한계(롱런): VO2max × 75%
  - ACSM 러닝 방정식 역산으로 속도 → 페이스(sec/km) 변환
- **권장 운동 거리 산출**:
  - BMI, 체지방률, 운동 경험 기반 고위험 여부 판정
  - 고위험: 템포 12분 / 롱런 30분 기준
  - 일반: 템포 20~25분 / 롱런 50~75분 기준

### 4. 맞춤 페이스 결과 화면
- 권장 페이스 범위 대형 표시 (예: 5'30" ~ 6'45")
- 페이스 바(bar) 시각화: 전체 구간(4'00"~12'00") 중 권장 구간 강조
- 템포런 / 롱런 권장 거리 카드 표시
- VO2max 수치 및 분석 근거 표시 (나이, 성별, 운동 경험)
- 인바디 보정 적용 여부 뱃지 표시
- 러닝 중 음성 코칭 안내 문구

### 5. 홈 화면
- 내 맞춤 페이스 카드 (페이스 범위 + VO2max + 인바디 반영 여부)
- 러닝 시작 버튼
- 최근 러닝 기록 목록 (Firebase Realtime Database 실시간 스트림)
  - 날짜, 거리, 시간, 평균 페이스, 칼로리 표시
  - 기록 없을 시 안내 문구 표시
- 로그아웃 버튼 (로그아웃 시 자동으로 로그인 화면 전환)

### 6. 실시간 러닝 화면 (GPS 기반)
- GPS로 실시간 페이스(sec/km), 이동 거리(km), 경과 시간 측정
- 현재 페이스를 대형 숫자로 표시 (색상으로 구간 구분)
  - 빨간색: 너무 빠름 (목표보다 빠름)
  - 초록색: 적정 페이스 (권장 구간 내)
  - 노란색: 너무 느림 (목표보다 느림)
- 상태 배너: "페이스를 낮춰보세요 / 완벽한 페이스! / 조금 더 힘내보세요"
- 목표 페이스 범위 상단 표시
- 시작 / 일시정지 / 재개 / 종료 컨트롤 버튼
- 종료 시 확인 다이얼로그

### 7. TTS 음성 코칭
- 러닝 시작 시 목표 페이스 음성 안내
- 페이스 이탈 감지 시 즉시 음성 알림 (15초 쿨다운으로 과도한 알림 방지)
  - "페이스가 너무 빠릅니다. 조금 천천히 달려보세요."
  - "완벽한 페이스예요! 이 속도를 유지하세요."
  - "조금 더 힘을 내보세요. 페이스를 올려볼까요?"
- 1km 통과 시 자동 음성 안내 (페이스 + 경과 시간)
- 일시정지 / 재개 음성 안내
- 러닝 완료 시 결과 음성 요약

### 8. 러닝 결과 화면
- 총 거리(km), 운동 시간, 평균 페이스(/km), 소모 칼로리(kcal) 카드 표시
- 러닝 중 페이스 변화 그래프 (CustomPaint로 직접 구현)
  - 라인 차트 + 그라데이션 채움 영역
- 홈으로 이동 버튼

### 9. Firebase Realtime Database 연동
- 신체 프로필 저장/불러오기 (`users/{uid}/profile`)
- 러닝 세션 저장/불러오기 (`users/{uid}/sessions/{sessionId}`)
- 실시간 스트림으로 기록 자동 갱신 (onValue 이벤트)
- 유저별 데이터 분리 (uid 기반)

---

## 기술 스택

| 분류 | 기술 |
|------|------|
| 프레임워크 | Flutter (Dart) |
| 상태관리 | Provider |
| 인증 | Firebase Authentication |
| 데이터베이스 | Firebase Realtime Database |
| 위치/GPS | Geolocator |
| 음성 | Flutter TTS |
| 알고리즘 | ACSM 러닝 방정식, 국내 VO2max 회귀식 |

---

## 앱 화면 흐름

```
로그인/회원가입
    ↓
신체 데이터 입력 (3단계)
    ↓
맞춤 페이스 결과 확인
    ↓
홈 화면 (페이스 카드 + 기록 목록)
    ↓
러닝 시작 → 실시간 GPS + TTS 코칭
    ↓
러닝 종료 → 결과 화면 → 홈으로
```

---

## 슬라이드 구성 제안

1. **표지** - 앱 이름, 팀명, 한 줄 소개
2. **문제 정의** - 러닝 입문자가 겪는 페이스 설정의 어려움
3. **해결 방안** - 신체 데이터 기반 맞춤 페이스 자동 산출
4. **핵심 기능 소개** - 위 기능 목록 기반 (화면별 스크린샷 삽입 권장)
5. **알고리즘 설명** - VO2max → 페이스 변환 파이프라인
6. **기술 스택** - Flutter, Firebase, GPS, TTS
7. **앱 화면 흐름** - 위 흐름도 기반
8. **시연 영상 또는 스크린샷**
9. **팀원 역할 분담**
10. **마무리 / Q&A**
